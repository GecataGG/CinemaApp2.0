<Window x:Class="CinemaApp.Wpf.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        Title="Login" Height="300" Width="400">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <!-- Email Input with Watermark -->
            <xctk:WatermarkTextBox Text="{Binding Email}" Width="300" Margin="10" Watermark="Email" />
            <!-- Password Input with PasswordChanged event -->
            <PasswordBox Name="PasswordBox" Width="300" Margin="10" PasswordChanged="PasswordBox_PasswordChanged"/>
            <!-- Login Button -->
            <Button Content="Log in" Command="{Binding LoginCommand}" Width="300" Margin="10" />
        </StackPanel>
    </Grid>
</Window>